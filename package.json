{
  "name": "cerebro-sync-server",
  "version": "1.0.0",
  "description": "CEREBRO // CODE RED — WebSocket Sync Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel": "node server.js"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.2.0",
    "ws": "^8.19.0"
  }
}
